# odoo_redis_storage

## Setup

Starting redis server command: ``` redis-server ```

Watching redis activities: ``` redis-cli monitor ```

Modules that need to be installed manually to access the shop: ``` Website, eCommerce ```